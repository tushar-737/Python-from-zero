tuple=("Alice","apple","banana","cherry")
count=tuple.count("apple")
print(count) # prints the number of occurrences of "apple" in the tuple
index=tuple.index("banana")
print(index) # prints the index of the first occurrence of "banana" in the tuple